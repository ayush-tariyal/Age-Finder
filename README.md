# Age-Finder
It's a web app to find the age of the person
It's a simple age finder that takes the user's birthyear as a input. It also tells the user their generation depending on their birthyear.

This file contains:
* HTML
* CSS
* JavaScript
